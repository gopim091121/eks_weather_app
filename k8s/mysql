CREATE TABLE messages (
  id INT AUTO_INCREMENT PRIMARY KEY,
  text VARCHAR(500) NOT NULL,
  user VARCHAR(100) NOT NULL DEFAULT 'Anonymous',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
  deleted TINYINT(1) DEFAULT 0
);

INSERT INTO messages (text, user) VALUES
('Hello from RDS MySQL!', 'System'),
('This is a realtime message', 'Gopi'),
('Learning Kubernetes with EKS 🚀', 'DevUser'),
('Socket.IO makes apps realtime', 'Frontend'),
('This message will be editable', 'Tester'),
('urgent: check backend logs', 'Admin');
